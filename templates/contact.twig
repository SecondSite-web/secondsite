{% extends 'index.twig' %} 

{% import 'form_macros.twig' as form %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col-md-12">

                <h1>Contact</h1>
                <p>Below is an example of how to build a form using Twig Macros. The Macros are written in 'form_inputs.twig.</p>
                <ul>
                    <li>input</li>
                    <li>inputLabelled</li>
                    <li>inputAppend</li>
                    <li>inputPreprend</li>
                    <li>textarea</li>
                </ul>
                <p>You can write custom macro's for form fields not included, or just add the form input field html to the macro list.
                    <br />"input(name, label, placeholder = null, options = []" - This is the value list passed to the input macro to generate the field parameters. </p>

                <form action="" method="post" id="" role="form" data-toggle="validator">
                    {{ form.input('name', 'Your name', 'Harry', {class: 'superclass'}) }}
                    {{ form.inputLabelled('name', 'Your name', 'Harry', {class: 'superclass'}) }}
                    {{ form.inputPrepend('name', 'Your name', 'placeholder', {class: 'superclass', require: 'required'}) }}
                    {{ form.inputAppend('name', 'Your name', 'placeholder', {class: 'superclass', require: 'required'}) }}
                    {{ form.input('email', 'Email', 'email@address.com') }}
                    {{ form.textarea('message', 'Message') }}
                <div class="form-group">
                    <div class="input-group date" id="datetimepicker1" data-target-input="nearest">
                        <input type="text" class="form-control datetimepicker-input" data-target="#datetimepicker1"/>
                        <div class="input-group-append" data-target="#datetimepicker1" data-toggle="datetimepicker">
                            <div class="input-group-text"><i class="fa fa-calendar"></i></div>
                        </div>
                    </div>
                </div>

            <script type="text/javascript">
                $(function () {
                    $('#datetimepicker1').datetimepicker();
                });
            </script>

                    <div class="form-group">
                        <button type="submit" id="submit" class="btn btn-primary">Send</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
{% endblock body %}

{% block scripts %}
<script type="text/javascript" src="{{ site.url }}vendor/moment/moment/min/moment.min.js"></script>
{# <script type="text/javascript" src="{{ site.url }}vendor/tempusdominus/bootstrap-4/build/js/tempusdominus-bootstrap-4.min.js"></script> #}
{% endblock scripts %}
